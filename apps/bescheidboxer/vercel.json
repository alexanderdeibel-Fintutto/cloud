{
  "framework": "vite",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . || exit 1",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
